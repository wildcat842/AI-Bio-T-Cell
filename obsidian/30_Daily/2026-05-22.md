---
title: "2026-05-22"
created: 2026-05-22
tags: [daily, setup, miniforge]
status: planned
---

# 2026-05-22 (Friday) - Miniforge 셋업 + 첫 검증

> 어제 결정: uv 단독 관리에서 **miniforge(conda-forge) 기반**으로 전환.
> 이유: 공용 서버에 R 미설치 → R+Python을 하나의 env로 통합 관리하려면 conda가 가장 합리적.

## 사전 준비
- [x] `Miniforge3-Linux-x86_64.sh` 다운로드 완료 (`~/Miniforge3-Linux-x86_64.sh`)
- [x] `environment.yml`에 R + bridge 통합 (이전 커밋)
- [x] `scripts/setup_miniforge.sh` 일괄 셋업 스크립트 작성
- [x] `datasets/01_reaptec_atlas/scripts/install_r_bridge.R` 작성 (sceasy/SeuratDisk)

## 오늘 체크리스트

### 1) Miniforge3 설치 (~10분)
- [x] 디스크 공간 확인: `df -h ~` (최소 5 GB 여유)
- [x] 기존 conda env가 있다면 백업: `conda env list` → 필요하면 `conda env export -n AI-Bio-T-Cell > ~/old_env.yml`
- [x] 셋업 스크립트 실행: `cd ~/work/AI-Bio-T-Cell && bash scripts/setup_miniforge.sh`
  - 스크립트가 자동으로:
    1. `~/Miniforge3-Linux-x86_64.sh` 실행 → `~/miniforge3` 설치
    2. `conda init bash` (새 셸부터 적용)
    3. `mamba env create -f environment.yml` (Python 3.12 + R 4.3 + GPU PyTorch + Bioconductor)
    4. `pip install -e ".[dev,ml,bridge,genome]"`
    5. `Rscript install_r_bridge.R` (sceasy, SeuratDisk)
    6. nvidia-smi / torch.cuda / Seurat / pytest 검증

### 2) 새 셸에서 env 활성화 확인 (~2분)
- [x] 새 터미널 열기 또는 `source ~/.bashrc`
- [x] `conda activate AI-Bio-T-Cell`
- [x] `python -V` → 3.12.x
- [x] `R --version` → 4.3.x
- [x] `nvidia-smi` → GPU 인식 확인 (서버 GPU 모델 기록:  NVIDIA-SMI 550.144.03             Driver Version: 550.144.03     CUDA Version: 12.4     |)

### 3) Python 측 점검 (~5분)
- [x] `python -c "import aibio, scanpy, torch, transformers; print('Python OK')"`
- [x] `python -c "import torch; print('CUDA', torch.cuda.is_available(), torch.cuda.get_device_name(0))"`
- [x] `pytest tests/python -q` → 전체 통과 확인
- [x] `python -c "from aibio.data import DATASET_REGISTRY; print(len(DATASET_REGISTRY))"` → 19

### 4) R 측 점검 (~5분)
- [x] `Rscript -e 'library(Seurat); library(Signac); library(reticulate); cat("R OK\n")'`
- [x] `Rscript -e 'reticulate::py_config()'` → conda env의 python 경로 가리키는지
- [x] `Rscript datasets/01_reaptec_atlas/scripts/install_r_bridge.R` → smoke test "OK" 확인

### 5) 데이터 다운로드 시작 (~30분~2시간, 네트워크 의존)
- [x] 디스크 여유 확인: `df -h .` (최소 60 GB; 22 GB 다운로드 + 22 GB unzip + 산출물)
- [x] `bash datasets/01_reaptec_atlas/scripts/download_dryad.sh --tier p0`
  - 진행 중 다른 작업 가능 (aria2c 멀티커넥션)
  - 이 부분은 수기로 브라우저에서 다운로드함
- [x] 완료 후 `bash datasets/01_reaptec_atlas/scripts/verify_checksums.sh`
- [x] `bash datasets/01_reaptec_atlas/scripts/unzip_and_organize.sh`

### 6) 첫 .rds 로딩 시도 (~10분)
- [x] R에서: `Rscript -e 'obj <- readRDS("datasets/01_reaptec_atlas/processed/seurat_objects/<첫_파일>.rds"); print(obj)'`
	- 가장 작은 rds 는 다음과 같다  Meckiff_etal_Cell_0h.rds
	- SMALLEST=$(find ~/work/AI-Bio-T-Cell/datasets/01_reaptec_atlas/processed/seurat_objects \ -name "*.rds" -printf "%s %p\n" | sort -n | head -1 | awk '{print $2}') echo "Smallest .rds: $SMALLEST" ls -lh "$SMALLEST"
	- Rscript -e " obj <- readRDS('$SMALLEST') cat('class:', paste(class(obj), collapse='/'), '\n') print(obj) " 2> >(grep -vE "activate: No such|CondaError" >&2)
  - 메모리 사용량 확인 (`htop` 별도 창에서)
		Avg CPU:     59.7%                ← 적절히 사용 중
			Mem:         177G / 976G          ← ★★★ 800G 여유 (18% 사용)
			Swap:        16G / 16G            ← ⚠️ swap 가득 (이전 메모리 압박의 잔재)
			Tasks:       1811 / 14418 threads ← 많지만 정상
			Running:     5                    ← CPU 동시 실행 중인 건 5개뿐
			Load avg:    94.19 / 82.98 / 80.92 ← 높지만 다중 코어라 OK (CPU 코어 수가 80+면 정상)
			Uptime:      263 days             ← 서버 안정 (재부팅 안 됨)
  - cell/feature 수, assay 종류 메모: 19311 유전자 24059 셀, RNA3개x2 (원본 + 로그스케일) + 노말라이즈 : 7개 레이어, 차원축소 6개 제시
  - 19311 features across 24059 samples within 1 assay
  - Active assay: RNA (19311 features, 2000 variable features)
  - 7 layers present: counts.SRR12664068_0h, counts.SRR12664069_0h, counts.SRR12664070_0h, data.SRR12664068_0h, data.SRR12664069_0h, data.SRR12664070_0h, scale.data
  - 6 dimensional reductions calculated: pca, umap.unintegrated, harmony, umap.harmony, integrated.cca, umap.cca
- [ ] `Rscript datasets/01_reaptec_atlas/scripts/rds_to_h5ad.R --max_size_gb 4` → subset별 변환 시도
- [ ] `python datasets/01_reaptec_atlas/scripts/verify_h5ad.py` → shape / obs / raw vs normalized 확인

### 7) 회고 + 다음 단계 계획 (~5분)
- [x] 실제 소요 시간 기록 : 48시ㅏㄴ
	- [ ] 막힌 부분 / 트러블슈팅 메모
- [ ] [[../70_Decisions/]]에 새 ADR 필요한지 판단 (예: R 패키지 빌드 실패 → 시스템 라이브러리 의존성 추가)
- [ ] [[2026-05-25]] 또는 다음 작업일 daily 작성

## 안전 망 (문제 발생 시)

| 증상 | 해결 |
| --- | --- |
| `pytorch-cuda =12.4 does not exist` (실측 발생) | 수정됨. `environment.yml`의 PyTorch가 pip + `--extra-index-url https://download.pytorch.org/whl/cu124`로 이동. `git pull` 후 `mamba env update -f environment.yml --prune` 재실행 |
| `mamba env create` 가 1시간+ 걸림 | Ctrl+C → `CONDA_SOLVER=libmamba mamba env create -f environment.yml` |
| `reticulate`가 시스템 python을 잡음 | `~/.Renviron`에 `RETICULATE_PYTHON=~/miniforge3/envs/AI-Bio-T-Cell/bin/python` |
| CUDA 버전 변경 (드라이버가 12.1 또는 12.6) | `environment.yml`의 `--extra-index-url ...whl/cu124` → `cu121` 또는 `cu126`. `mamba env update --prune` |
| `Seurat` 설치 실패 | `mamba install -n AI-Bio-T-Cell -c conda-forge r-seurat` 단독 시도 |
| `sceasy` GitHub 설치 실패 (의존성) | R 안에서 `BiocManager::install(c("LoomExperiment"))` 먼저 |
| 다운로드 중단 | 같은 명령 재실행 (aria2c가 자동 이어받기) |
| 디스크 부족 | Micro-C(`DataS7`)는 P2 tier - P0만 받음. `mamba clean -a -y` |

## 자료

- 환경 설계 보고서: `reports/deliverables/environment_design.md`
- ReapTEC 데이터 README: `datasets/01_reaptec_atlas/README.md`
- Bridge 방법: [[../60_Methods/R_Python_Bridge]]
- DNABERT-2: [[../60_Methods/DNABERT2_Embedding]]
- GNN: [[../60_Methods/GNN_Enhancer_Network]]

## 시간 예산 (참고)
- 합계 권장: 반나절 (오전 4시간) 정도로 잡되, 데이터 다운로드는 백그라운드 진행
- mamba env create가 가장 오래 걸림 (15~30분, 네트워크/디스크 의존)

## 회고 (저녁에 채울 것)

### 잘 된 것
- 클로드로 obsidian 업데이트, VPN 리눅스로 작업하고 처리- 문제가 있으면, 패치 등으로 쉽게 수정하고, 확인하고 다시 리눅스에서 git commit 하는 패턴이 잘 되고 있음

### 어려웠던 것
- 환경 설정 :  venv 를 사용하였으나 R 통합 환경을 고려하여 conda 로 결정하면서 기존의 python 이 실행되는 등 혼선이 있었음. -> 루트의 .venv 는 삭제  
- 데이터 다운로드:  dryand(?) 파일 URL 로 받았으나 you may not ... 으로 거부 당함

### 다음 작업일 (예: 2026-05-25)
[] sauret 구조를 좀더 파악하고, 
[] 메모리 사용량 모니터링 하면서 시각화 부터 차근히 진행할 것
[] R 에서 라이브러리 설치 방법이 있는지 (install.package 말고 pip 나 conda 같은)
